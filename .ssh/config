# When you ssh into your work machine from home, you need to be able to fetch
# from remote repositories without constantly entering passwords.
#
# Automatically add keys to a running ssh-agent when you use ssh. This avoids
# manually adding they key with ssh-add:
# https://unix.stackexchange.com/a/12201/233125
AddKeysToAgent yes
